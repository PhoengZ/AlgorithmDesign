#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main(){
    int n;
    cin>> n;
    vector<int> vp(n);
    for (int i = 0;i<n;i++)cin >> vp[i];
    
    return 0;
}